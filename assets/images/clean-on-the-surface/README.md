# Clean on the Surface image directory

The current editorial preview references the uploaded image masters from the repository root. These files will be consolidated into this directory during the final production cleanup after the preview layout is approved.
