# Genki2 asset repair

The canonical Genki2 renderer now loads a verified base64 WebP payload at runtime. This avoids the corrupt binary asset that caused the robot to disappear. Mobile, tablet, desktop, and the arrival sequence use the same payload.
